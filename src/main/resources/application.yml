spring:
  datasource:
    username: root
    password: tiger
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://db-server:3307/test
  data:
    jpa:
      repositories:
        enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
#    database-platform: org.hibernate.dialect.MySQLDialect
#    database: MYSQL
  freemarker:
    suffix: html
    request-context-attribute: request
#  profiles:
#    active: dev
#mybatis:
#  config-location: conf/mybatis-config.xml
#  mapper-locations: classpath:conf/mapper/*.xml
#  type-aliases-package: com.crawler.crawler.model
server:
  port: 80
  servlet:
    context-path: /perplayer
  tomcat:
    uri-encoding: UTF-8
